{
  "name": "znap-cdk-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "deploy": "turbo deploy",
    "deploy:prev": "turbo deploy prev",
    "deploy:prod": "turbo deploy:prod",
    "synth": "turbo synth",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev:dynamodb": "cmd /c  dynamodb-setup-dev.bat"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.0",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
